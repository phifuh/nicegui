services:
  - type: web
    name: rte-demo
    env: python
    pythonVersion: "3.13"
    buildCommand: uv pip install --system .
    startCommand: python demo.py
    envVars:
      - key: STORAGE_SECRET
        generateValue: true
